---
import Layout from '../layouts/Layout.astro';
import EstimatorPrototypeApp from '../components/prototype/EstimatorPrototypeApp';
import { loadPrototypeSeedData } from '../lib/prototypeSeedLoader';

const seedData = await loadPrototypeSeedData();
---

<Layout title="Civils Quote Workspace">
  <EstimatorPrototypeApp client:load seedData={seedData} />
</Layout>
